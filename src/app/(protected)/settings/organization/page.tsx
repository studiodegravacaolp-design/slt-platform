import Link from 'next/link'; import { getCurrentOrganization } from '@/services/organizations'; import { saveOrganization } from '../actions'
export default async function OrganizationPage() { const o = await getCurrentOrganization(); return <><Link href="/settings">← Configurações</Link><h1>Organização</h1><form action={saveOrganization} className="student-form"><section className="form-section"><div className="form-grid"><label className="span-2">Nome<input name="name" defaultValue={o.name} /></label><label>Nome fantasia<input name="trade_name" defaultValue={o.trade_name ?? ''} /></label><label>CNPJ<input name="cnpj" defaultValue={o.cnpj ?? ''} /></label><label>E-mail<input name="email" defaultValue={o.email ?? ''} /></label><label>Telefone<input name="phone" defaultValue={o.phone ?? ''} /></label><label className="span-2">Site<input name="website" defaultValue={o.website ?? ''} /></label></div></section><button>Salvar organização</button></form></> }
