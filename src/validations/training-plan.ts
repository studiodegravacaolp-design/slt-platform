import { z } from 'zod'
export const trainingSchema = z.object({ name: z.string().trim().min(1), description: z.string().trim().min(1).optional(), objective: z.string().trim().min(1).optional(), observations: z.string().trim().min(1).optional(), status: z.string().trim().min(1).optional(), start_date: z.iso.date().optional(), end_date: z.iso.date().optional(), student_modality_unit_id: z.coerce.number().int().positive() })
export const trainingExerciseSchema = z.object({ exercise_name: z.string().trim().min(1), sets: z.coerce.number().int().positive().optional(), repetitions: z.coerce.number().int().positive().optional(), load: z.coerce.number().nonnegative().optional(), rest_time: z.coerce.number().int().nonnegative().optional(), duration: z.coerce.number().int().nonnegative().optional(), intensity: z.enum(['leve','moderada','alta']).optional(), observations: z.string().trim().min(1).optional(), order: z.coerce.number().int().nonnegative() })
