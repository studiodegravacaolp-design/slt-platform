import Link from 'next/link'; import { listUnits } from '@/services/units'; import { saveUnit } from '../actions'
export default async function UnitsPage() { const units = await listUnits(); return <><Link href="/settings">← Configurações</Link><h1>Unidades</h1><section className="panel">{units.length ? <ul>{units.map(u => <li key={u.id}>{u.name} · {u.status}{u.is_main ? ' · Principal' : ''}</li>)}</ul> : <p>Nenhuma unidade cadastrada.</p>}</section><form action={saveUnit} className="student-form"><section className="form-section"><h2>Nova unidade</h2><div className="form-grid"><label>Nome *<input name="name" required /></label><label>Código<input name="code" /></label><label>E-mail<input name="email" /></label><label>Telefone<input name="phone" /></label><label>Status<input name="status" /></label><label>Principal<input name="is_main" type="checkbox" /></label></div></section><button>Criar unidade</button></form></> }
