import Link from 'next/link'; import { notFound } from 'next/navigation'; import { createClient } from '@/lib/supabase/server'; import { saveTrainingAction } from '../../actions'
export default async function EditTraining({params}:{params:Promise<{id:string}>}){const id=Number((await params).id);const s=await createClient();const {data:t,error}=await s.from('trainings').select('*').eq('id',id).single();if(error||!t)notFound();return <><Link href={`/trainings/${id}`}>← Treinamento</Link><h1>Editar treinamento</h1><form action={saveTrainingAction.bind(null,id)} className="student-form"><label>Nome<input name="name" defaultValue={t.name}/></label><label>Descrição<textarea name="description" defaultValue={t.description??''}/></label><label>Objetivo<input name="objective" defaultValue={t.objective??''}/></label><label>Observações<textarea name="observations" defaultValue={t.observations??''}/></label><label>Início<input name="start_date" type="date" defaultValue={t.start_date??''}/></label><label>Fim<input name="end_date" type="date" defaultValue={t.end_date??''}/></label><label>Status<input name="status" defaultValue={t.status}/></label><button>Salvar</button></form></>}
